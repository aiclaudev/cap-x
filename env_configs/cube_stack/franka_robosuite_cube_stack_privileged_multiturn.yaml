# Usage: uv run capx/envs/launch.py \
#   --config-path env_configs/cube_stack/franka_robosuite_cube_stack_privileged_multiturn.yaml \
#   --model Qwen/Qwen2.5-Coder-7B-Instruct \
#   --server-url http://127.0.0.1:8080/v1/chat/completions
#
# Privileged perception (ground-truth poses -> no gated SAM3 / GraspNet needed, PyRoKi only)
# + multi-turn so the agent regenerates code across turns. Good first local-vLLM smoke run.

env:
  _target_: capx.envs.tasks.franka.franka_pick_place.FrankaPickPlaceCodeEnv
  cfg:
    _target_: capx.envs.tasks.base.CodeExecEnvConfig
    low_level: franka_robosuite_cubes_low_level
    privileged: true
    apis:
      - FrankaControlPrivilegedApi
    multi_turn_prompt: |
      The following code blocks were just executed:
      ```python
      {executed_code}
      ```
      The current console stdout is:
      ```
      {console_stdout}
      ```
      The current console stderr is:
      ```
      {console_stderr}
      ```
      If there were no errors during compilation or execution, the console stderr will be empty. If there were errors, consider regenerating the code.
      Consider querying the environment state through perception-based tool calls and prints to see if the task is completed.
      Please respond with EXACTLY ONE of the following:
      - The word 'REGENERATE' followed immediately by new Python code in a fenced code block (```python...```) if you want to modify the code.
      - The word 'FINISH' if the task appears to be complete

api_servers:
  - _target_: capx.serving.launch_pyroki_server.main
    port: 8116
    host: 127.0.0.1
    robot: panda_description
    target_link: panda_hand

record_video: true
use_legacy_multi_turn_decision_prompt: true
output_dir: ./outputs/cube_stack_privileged_multiturn_qwen

trials: 2
num_workers: 1
