# agentv1 (Reflector) — cube_lifting, 1 trial. Generated from the reduced_api_skill_lib config.
# code + reflector = gemini-3.1-pro-preview @ think low (CLI overrides model/gateway for letsur).
env:
  _target_: capx.envs.tasks.franka.franka_lift.FrankaLiftCodeEnv
  cfg:
    _target_: capx.envs.tasks.base.CodeExecEnvConfig
    low_level: franka_robosuite_cube_lift_low_level
    privileged: false
    apis:
    - FrankaControlApiReducedSkillLibrary
    prompt: 'You are controlling a Franka Emika robot with API described below.

      Goal: pick up the red cube and lift it.

      You may write python code comments for reasoning but ONLY write the executable Python code and do not write it in code fences.

      After this code executes, you will receive the current console stdout and stderr (any errors or print statements made in the code). You will then have the chance to write new code to query the environment state and/or complete the task.

      The functions (APIs) below are already imported to the environment. If you want to use numpy, you need to import it explicitly.

      '
    multi_turn_prompt: 'The following code blocks were just executed:

      ```python

      {executed_code}

      ```

      The current console stdout is:

      ```

      {console_stdout}

      ```

      The current console stderr is:

      ```

      {console_stderr}

      ```

      '
api_servers:
- _target_: capx.serving.launch_sam3_server.main
  device: cuda
  port: 8114
  host: 127.0.0.1
- _target_: capx.serving.launch_contact_graspnet_server.main
  port: 8115
  host: 127.0.0.1
- _target_: capx.serving.launch_pyroki_server.main
  port: 8116
  host: 127.0.0.1
  robot: panda_description
  target_link: panda_hand
model: google/gemini-3.1-pro-preview
reflector_model: google/gemini-3.1-pro-preview
reasoning_effort: low
use_reflector: true
version: agentv1
record_video: true
save_multiturn_prompts: true
output_dir: ./outputs/franka_robosuite_cube_lifting
trials: 1
num_workers: 1
